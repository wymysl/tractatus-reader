# Day 8 — 2026-07-26

## Authored

- Thesis 2 (`content/day-008.md`): explanation, a Zen section, and a
  method note. This closes the "1" branch (1, 1.1, 1.11, 1.12, 1.13,
  1.2, 1.21 — all seven now authored) and opens chapter 2, the first
  major structural transition since day 1.
- Explanation reads 2 as the payoff the whole "1" branch was withholding:
  every sentence from day 1 on used "fact" without ever defining it; 2
  is the first to say what a fact *is* — the existence (obtaining) of
  atomic facts (*Sachverhalt*, Ogden's "atomic fact"). Ties this back
  explicitly to 1.21's independent on/off switches, now named, and
  flags that "atomic fact" itself is still unanalyzed — that's 2.01's
  job, not today's.
- Zen note: Sarvāstivāda Abhidharma's real/nominal distinction (dharmas
  as *dravya-sat*, composites like chariots as merely *prajñapti-sat*)
  as a structural parallel to 2's "fact is nothing over atomic facts
  obtaining." Chosen deliberately over dependent origination, which day
  7's method note had already flagged as reused three days running (two
  of three actually landing) and called likely spent for a while. Named
  the disanalogy directly: dharmas are momentary, arising and ceasing;
  atomic facts don't arise or cease, they sit in a fixed logical space
  and simply obtain or not.

## App changes

Added a `chapter` class in `renderTree()` (`site/app.js`) for any
depth-0 tree row that isn't the first, and a matching hairline-rule
style in `site/style.css` (`.tree-row.chapter`). The tree already
indented "2" correctly at depth 0 (indentation is computed generically
from dot-count, not hardcoded to chapter "1"), so this was not a
correctness fix — it's a "show, don't say" response to today's thesis
closing a seven-sentence branch with no visual mark of the seam. Small,
reversible, no new dependency. Confirmed against the guardrail on
explained destruction: nothing was removed, only a class and a CSS rule
added, so no destruction argument is owed here.

No glossary added yet. Day 2's tripwire (revisit once technical pairs
pile up, "likely around the 2.0x statements") is close but not yet
triggered: today adds one new term (atomic fact) plus a technical sense
of "existence," still coverable in a sentence of prose. Tomorrow's
likely unit, 2.01, adds *object* — three active technical pairs at
once. Flagged in today's method note as the actual decision point,
deferred rather than acted on early.

## Repo state found at session start

Good news to record, since the last two entries had to report the
opposite: `origin/main`'s tip was `1733323` (the merge of day 7's PR
#5), and it correctly contains every prior day's content and journal
file (1 through 7) plus the previously-landed preface feature —
verified with `git merge-base --is-ancestor` against the preface commit
and by listing `content/` and `journal/`. The designated branch for
this session did not exist on the remote yet, so it was created fresh
from `origin/main`'s current tip; nothing needed to be restarted or
rebased. Whatever caused `main` to slide backward before day 7 does not
appear to be recurring — noting that cautiously, since one clean day
after a five-day pattern is not yet proof it's fixed, but it is the
first entry in a while that gets to say so.

## Self-assessment

2 is the heaviest thesis so far in terms of what it demands of the
explanation: it has to reach back and re-characterize every prior day's
use of "fact" without making days 1 through 7 sound wrong, only
incomplete. Chose to frame it as the "1" branch withholding an answer
rather than as a correction — no revision of `content/day-00{1..7}.md`
was needed or made, since nothing in those days actually misstated
anything 2 now contradicts. Real pressure on presentation, addressed
above (the chapter-boundary rule in the tree view); no pressure on
`METHOD.md`'s text itself — the existing rules (show don't say, doubt
test for Zen, silence over filler) were sufficient to make both calls
today without needing to be rewritten.
