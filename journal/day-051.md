# Day 51 — 2026-09-23

## Authored

- Thesis 2.11 (`content/day-051.md`): explanation and method note, no Zen
  note.
- Explanation identifies 2.11 as the payoff of two standing forward
  debts: day 19's forecast that *Sachlage* would surface as what a
  picture presents (2.11, 2.202), and day 5's forecast that "logical
  space" would recur at this exact sentence. Both confirmed against
  `data/tractatus.json` rather than trusted from memory. New textual
  finding: 2.11's clause "das Bestehen und Nichtbestehen von
  Sachverhalten" is a verbatim repetition of 2.06's own definition of
  reality, confirmed by direct comparison of the two entries — argued
  this is why a picture's content is built to be commensurable with
  reality (so that later agreement or disagreement, 2.21/2.222, is even
  askable), and why the word must be *Sachlage* rather than *Sachverhalt*
  (too small a unit) or *Tatsache* (already committed to actuality, per
  thesis 1). Extended day 19's flagged Ogden inconsistency for
  *Sachlage* ("state of affairs" / "circumstances") with a third,
  more consequential rendering here — "facts" — which collides with
  Ogden's own word for the different, already-established *Tatsache*,
  risking a reader importing thesis 1's committed sense into a word that
  specifically avoids it.
- Declined the interpretive-war paragraph: 2.11 continues the apparatus
  2.1 already flagged as the sharpest fork so far, without newly
  dividing the two readings.
- Declined the Zen note on the doubt test: checked the *Yijing*'s
  hexagram (existence/non-existence as broken/unbroken lines, a fixed
  sixty-four-configuration space) and Nyāya's *yathārtha*/*ayathārtha*
  cognition distinction; both fell short (the hexagram doesn't clearly
  separate representer from represented the way 2.11's grammar does;
  the Nyāya parallel is with correspondence theories of truth generally,
  true but unsurprising). Today's real content is textual, not
  metaphysical.

## App changes

Fixed a live rendering bug, found while checking 2.11's own English text
against the data file: `data/tractatus.json` carries TeX's discretionary
hyphen (`\-`) verbatim in 37 of 526 entries, including two
already-published days (13: 2.0233; 45: 2.05) and today's own 2.11
("non-ex\-is\-tence"). `site/app.js` inserts statement text straight into
the DOM, so the mark rendered as a literal visible backslash instead of
vanishing as the original typesetting intended. Added
`stripDiscretionaryHyphens` to `build.mjs` — a pure function, tested
directly — applied to every statement's `en`/`de` text when it is
written into each day's unit JSON and into `tree.json`. `data/tractatus.json`
itself is untouched; the fix runs at build time on generated output
only, consistent with the immutability guardrail. Added one unit test for
the function and one build-level test confirming the artifact is gone
from emitted files (`npm test`: 32 passing, up from 30). `node build.mjs`
green before committing.

This changes the rendered text of days 13 and 45 (and every other
affected already-published day) without touching their `content/` files
— flagged per METHOD.md's confession rule for revising the past, since
it is the rendered presentation of prior days that changes, not what
they say.

Found, and left alone, a related but separate artifact class in the same
data file (`\[`, `\]`, `\ ` tangled with stray `<pre>` blocks) — but only
in entries far past the current frontier (4.1252, 5.451, 6.02). Not
today's job: those pages are years away at this pace, and fixing markup
I can't yet see rendered in context isn't proportionate to what 2.11
calls for. Named in today's method note so it isn't rediscovered cold.

Confirmed (no change needed) that `computeDepths` places 2.11 correctly
at depth 2, under 2.1 — the plain digit-count case, not a zero-padding
one, since 2.1 carries no leading zero.

## Self-assessment

The real work of the day was catching up two long-standing forecasts and
a live, already-deployed rendering bug that today's own thesis text
happened to trigger — closer in shape to day 50's day-9 recheck than to
a routine content day. Checked directly, rather than assumed, that the
hyphen fix is proportionate in scope: it touches only the artifact
2.11 itself exhibits and that recurs identically elsewhere (a single
regex, no chapter-specific handling), and it explicitly declines to
chase the structurally different, not-yet-relevant artifact class found
in the same grep. That mirrors day 50's own reasoning for judging its
tree-depth fix proportionate — recurrence within the same simple pattern
justifies a general fix; a different, harder problem in unreached
territory does not.

Zen-source diversity checked against the last ten days' full or
considered sources (41: Saussure, 42: Aristotle reused, 43: Broad/Mill,
44: Democritus reused, 45: Tao Te Ching, 46: Spinoza/Hegel, 47: dependent
origination, 48: al-Ghazali, 49: Chandogya/Vedanta, 50: Vasubandhu
reused): today's two considered-and-declined candidates (*Yijing*,
Nyāya) are both first considerations by grep, not repeats — the decline
was on the merits (the doubt test), not availability.

No METHOD.md change: today's calls — cashing two forecasts, confessing a
past-rendering revision, declining the concordance tool again on a
sharper ground, declining interpretive-war and Zen — all ran under rules
already on the books.

This PR will be left unmerged per ROUTINE.md — human review pending.
Cloudflare has not deployed today's unit yet.
